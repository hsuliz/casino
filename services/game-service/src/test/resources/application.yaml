spring:
  application:
    name: game
  grpc:
    server:
      enabled: false
  threads:
    virtual:
      enabled: true
  data:
    mongodb:
      uri: mongodb://root:dumb-pass@mongodb:27017/mydb?authSource=admin
